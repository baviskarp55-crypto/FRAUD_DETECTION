# FRAUD_DETECTION
Fraud detection system using a full machine learning pipeline on a transaction dataset with features like amount, hour, frequency, account age, and international flag. Trains and evaluates Logistic Regression, Decision Tree, Random Forest, SVM, and KNN, plus a VotingClassifier ensemble, and saves the final model for deployment
